/**
 * swagger.ts
 *
 * Swagger/OpenAPI documentation for the agent API.
 * Mount at: /api/docs
 *
 * Note: Requires swagger-jsdoc and swagger-ui-express to be installed.
 * Install with: npm install swagger-jsdoc swagger-ui-express
 */

import { Express } from 'express';

export function setupSwagger(_app: Express): void {
  // Swagger requires additional dependencies. Install with:
  // npm install swagger-jsdoc swagger-ui-express
  // This is a placeholder — uncomment when dependencies are added.
}

// Made with Bob
